components:
  schemas:
    DataStorageLoSCapabilities:
      anyOf:
      - $ref: http://redfish.dmtf.org/schemas/v1/odata.v4_0_3.yaml#/components/schemas/idRef
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_0_0.yaml#/components/schemas/DataStorageLoSCapabilities
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_0_1.yaml#/components/schemas/DataStorageLoSCapabilities
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_1_0.yaml#/components/schemas/DataStorageLoSCapabilities
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_1_1.yaml#/components/schemas/DataStorageLoSCapabilities
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_1_2.yaml#/components/schemas/DataStorageLoSCapabilities
      - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.v1_2_0.yaml#/components/schemas/DataStorageLoSCapabilities
      description: Describe data storage capabilities
      x-longDescription: This resource shall be used to describe storage availability
        and provisioning capabilities.
    ProvisioningPolicy:
      description: Space provisioning policy.
      enum:
      - Fixed
      - Thin
      type: string
      x-enumDescriptions:
        Fixed: Storage is fully allocated
        Thin: Storage may be over allocated.
      x-enumLongDescriptions:
        Fixed: This enumeration literal specifies storage shall be fully allocated.
        Thin: This enumeration literal specifies storage may be over allocated.
      x-longDescription: The enumeration literals may be used to specify space provisioning
        policy.
    StorageAccessCapability:
      description: Values of StorageAccessCapability describe abilities to read or
        write storage.
      enum:
      - Read
      - Write
      - WriteOnce
      - Append
      - Streaming
      - Execute
      type: string
      x-enumDescriptions:
        Append: AppendOnly.
        Execute: Execute access is allowed by the file share.
        Read: Read.
        Streaming: Streaming.
        Write: Write Many.
        WriteOnce: WriteOnce.
      x-enumLongDescriptions:
        Append: This enumeration literal shall indicate that the storage may be written
          only to append.
        Execute: This value shall indicate that Execute access is allowed by the file
          share.
        Read: This enumeration literal shall indicate that the storage may be read.
        Streaming: This enumeration literal shall indicate that the storage may be
          read sequentially.
        Write: This enumeration literal shall indicate that the storage may be written
          multiple times.
        WriteOnce: This enumeration literal shall indicate that the storage may be
          written only once.
      x-longDescription: StorageAccessCapability enumeration literals may be used
        to describe abilities to read or write storage.
title: '#DataStorageLoSCapabilities.DataStorageLoSCapabilities'
x-copyright: Copyright 2016-2018 Storage Networking Industry Association (SNIA), USA.
  All rights reserved. For the full SNIA copyright policy, see http://www.snia.org/about/corporate_info/copyright
x-owningEntity: SNIA
